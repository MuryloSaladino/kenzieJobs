import styled from "styled-components";

export const StyledMain = styled.main`
    max-width: 1328px;
    margin: 0 auto 120px ;

    div {
        display: flex;
        align-items: center;
        gap: 13px;
    }

    section {
        margin: 0 auto;
    }
`