import styled from "styled-components";

export const StyledCreateJobForm = styled.form`
    width: 100%;
    max-width: 542px;
    margin: 0 auto;

    display: flex;
    flex-direction: column;
    gap: 17px;

    button{
        display: flex;
        align-items: center;
        gap: 10px;

        align-self: flex-end;
    }
`